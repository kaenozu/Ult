# Quality Gatekeeper Skill
## 自動化ルール
1. **変更即検証**: `replace` 等でコードを変更した後、指示を待たずに `npm run build` を実行。
2. **自己修正**: ビルドエラー時はエラー内容を即座に解析し、修正案を自ら作成・適用する。
3. **退行防止**: 過去に修正した不整合（ラベルと予報線の不一致等）が再発していないか自動検閲する。
