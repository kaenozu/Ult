{
  "name": "chain-commands",
  "description": "Executes multiple shell commands sequentially in a cross-platform manner. Stops if any command fails. Use this instead of '&&' operators.",
  "commands": [
    {
      "name": "run",
      "usage": "node skills/chain-commands.js \"<command1>\" \"<command2>\" ...",
      "description": "Runs the provided commands in order."
    }
  ]
}
