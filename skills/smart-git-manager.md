# Smart Git Manager Skill
## 自動化ルール
1. **完全非対話**: `gh pr create` 等の対話コマンドは、フラグをフル活用して「入力待ち」を100%回避する。
2. **自動プッシュ**: 変更が完了し、品質検証(Quality Gatekeeper)を通ったら、即座にブランチ作成・プッシュを行う。
3. **環境同期**: リモートの最新状態を常にバックグラウンドで監視し、必要なら自動でRebaseする。
