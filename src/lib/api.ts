// API client utilities
export async function executeTrade(data: any) {
  // Placeholder implementation
  console.log('Executing trade:', data);
  return { success: true };
}

// Re-export types for convenience
export type { TradeRequest } from '@/types';
