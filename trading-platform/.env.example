# ═══════════════════════════════════════════════════════════════════════════════
# ⚠️  SECURITY WARNING: NEVER COMMIT ACTUAL API KEYS TO THE REPOSITORY ⚠️
# ═══════════════════════════════════════════════════════════════════════════════
#
# This file is a template. Copy it to .env.local for local development.
#
# Setup instructions:
# 1. Get your free API key at: https://www.alphavantage.co/support/#api-key
# 2. Copy this file to .env.local (already in .gitignore)
# 3. Replace 'your_api_key_here' with your actual API key
# 4. NEVER commit .env.local to the repository
#
# For production deployment:
# - Set ALPHA_VANTAGE_API_KEY in your hosting platform's environment variables
# - Examples: Vercel (Project Settings > Environment Variables)
#             AWS Lambda (Environment variables)
#             Docker (docker-compose.yml or Dockerfile ENV)
#             Kubernetes (ConfigMap/Secret)
# ═══════════════════════════════════════════════════════════════════════════════

# Alpha Vantage API Configuration
# IMPORTANT: Use ALPAH_VANTAGE_API_KEY (NOT NEXT_PUBLIC_*) to keep it server-side only
ALPHA_VANTAGE_API_KEY=your_api_key_here
