name: Minimal Test

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World"
      - run: gh --version
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
